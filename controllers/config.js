//defaultConfig.js
const config = {
    // 数据库配置
    database: {
        DATABASE: 'yublog', //数据库名称
        USERNAME: 'root', //mysql用户名
        PASSWORD: 'yu325914', //mysql密码
        PORT: '3306', //mysql端口号
        HOST: 'localhost' //服务器ip
    }
}
module.exports = config